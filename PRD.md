# 项目 PRD：Windows 本地文件搜索工具（Findex）

> **版本**: v0.1 | **日期**: 2026-07-27 | **状态**: 草案

---

## 1. 背景与问题

### 1.1 用户痛点

- **Windows 原生搜索慢、资源占用高**：Windows Search 索引器后台常驻，CPU 和磁盘 I/O 消耗大，搜索结果延迟高
- **资源管理器层级导航低效**：深度嵌套文件夹需要逐层点击，定位文件效率低下
- **"打开文件"对话框无搜索能力**：第三方应用的 Save/Open 对话框缺乏快速搜索功能
- **缺少统一搜索体验**：桌面、资源管理器、应用对话框各有不同的搜索入口，互不打通

### 1.2 参考项目分析

| 项目 | 核心优势 | 核心局限 |
|------|---------|---------|
| **Everything** | 毫秒级搜索，内存占用极低（~10MB），USN Journal 增量索引，清晰 SDK/IPC | 无资源管理器集成，无上下文感知，纯文件名搜索，UI 较原始 |
| **Listary 6.x** | 上下文感知（当前文件夹优先），Quick Switch，Shell 集成，模糊/拼音搜索，插件体系 | 内存占用较高（~100MB），.NET Framework 依赖，闭源 |
| **Listary V7 Beta** | 新引擎（内存降30%，速度升20%），高级搜索语法，预览面板，类目筛选 | 仍处 Beta 阶段，底层架构未根本改变 |

---

## 2. 第一性原则分析

### 2.1 根本问题

**Windows 上定位文件的速度与用户意图之间的差距。**

### 2.2 根本约束

1. **物理极限**：NTFS USN Journal 记录所有文件系统元数据变更，读取速度远快于全盘扫描
2. **用户意图**：用户在特定上下文中（当前文件夹、最近访问、特定项目）寻找文件，而非全局无差别搜索
3. **交互成本**：从用户产生"找文件"的想法到看到结果，理想延迟 < 100ms，任何超时都会导致注意力流失

### 2.3 核心原则

1. **索引非搜索**：搜索是瞬间的，索引是持续的。索引不阻塞用户操作
2. **上下文优先**：搜索结果中，当前文件夹 > 全局匹配 > 模糊匹配
3. **资源最小化**：索引引擎常驻内存 < 30MB，CPU 空闲时 < 0.5%
4. **零学习成本**：搜索行为应直觉化，用户无需学习语法即可使用

---

## 3. 产品目标

### 3.1 愿景

构建融合 Everything 的秒级索引速度与 Listary 的场景感知搜索体验的轻量级 Windows 文件检索工具。

### 3.2 核心指标

| 指标 | 目标值 |
|------|--------|
| 首次搜索延迟 | < 50ms（冷启动后） |
| 内存占用（引擎空闲） | < 30MB |
| 索引增量更新延迟 | < 1s（文件变更后） |
| 初始全盘索引时间 | < 10s（100万文件） |
| UI 显示延迟 | < 15ms（热键按下到界面显示） |

### 3.3 目标用户

- **开发者**：快速在项目目录中定位代码文件
- **办公用户**：在深度嵌套文件夹中快速定位文档
- **普通用户**：替代 Windows 原生搜索

---

## 4. 功能需求

### 4.1 MVP（Phase 1）

#### 4.1.1 索引引擎

- **USN Journal 增量索引**：利用 `DeviceIoControl` + `FSCTL_QUERY_USN_JOURNAL` 读取 NTFS 变更记录
- **初始全盘扫描**：通过 `FindFirstFile`/`FindNextFile` 或 USN Journal 枚举建立初始索引
- **文件元数据索引**：文件名、路径、大小、创建日期、修改日期、访问日期、属性（只读/隐藏/系统）
- **文件夹索引**：支持监控指定卷/文件夹，排除路径/模式
- **持久化存储**：SQLite 存储索引快照，支持冷启动加载
- **实时监控**：`ReadDirectoryChangesW` 或 USN Journal 实时文件变更通知

#### 4.1.2 搜索能力

- **前缀匹配**：输入 "report" 匹配 "report.docx"、"presentation-report.pdf"
- **子串匹配**：输入 "port" 匹配 "report.docx"
- **路径分段匹配**：输入 "src/app" 匹配 "D:\project\src\app\main.rs"
- **拼音首字母**：输入 "bg" 匹配 "报告.docx"（需拼音词库）
- **后缀过滤**：输入 `.pdf` 或 `*.pdf`
- **大小写不敏感**（默认）

#### 4.1.3 搜索 UI（命令行原型）

- 命令行搜索工具 `findex.exe search <query>`
- 支持输出格式：纯文本列表、JSON
- 支持分页、排序

### 4.2 Phase 2

#### 4.2.1 搜索浮层 UI

- **全局热键**：双击 Ctrl 或可配置快捷键呼出搜索浮层
- **搜索浮层**：轻量级 WPF 窗口，半透明，居中或跟随鼠标
- **实时搜索**：输入即搜索，结果列表实时更新
- **结果高亮**：匹配部分高亮显示
- **文件图标**：显示文件类型图标
- **文件预览**：基本信息（大小、日期、路径）
- **键盘导航**：↑↓ 选择，Enter 打开，Ctrl+Enter 打开所在文件夹

#### 4.2.2 上下文感知

- **自动检测当前文件夹**：检测前台资源管理器窗口的当前路径
- **作用域搜索**：默认搜索当前文件夹及其子目录
- **全局搜索切换**：Tab 或特定快捷键切换为全盘搜索
- **智能排序**：当前文件夹结果优先，近期访问次之，全局匹配最后

#### 4.2.3 Quick Switch

- **Save/Open 对话框集成**：在标准文件对话框中通过 Ctrl+G 呼出快速跳转
- **路径片段搜索**：输入路径片段跳转到目标文件夹
- **收藏夹快速跳转**：支持收藏常用文件夹

### 4.3 Phase 3

#### 4.3.1 高级搜索

- **高级搜索语法**：`ext:pdf`、`date:today`、`size:>10mb`、`"exact phrase"`、`-exclude`
- **类目筛选**：按文档、图片、视频、音频分类
- **布尔运算**：AND、OR、NOT
- **正则表达式搜索**

#### 4.3.2 Shell 集成

- **资源管理器工具栏按钮**：在 Explorer 中嵌入搜索按钮
- **右键菜单集成**：在文件夹上右键→"在此搜索"
- **文件操作**：搜索结果中直接复制、删除、重命名、打开路径

#### 4.3.3 插件系统

- **文件应用插件**：类似 Listary FileAppPlugin，支持 WinRAR、WPS、7-Zip 等
- **Web 搜索插件**：在搜索框中直接搜索 Google、Baidu 等
- **计算器/转换器插件**：在搜索框中做简单计算

### 4.4 Phase 4

- 剪贴板历史搜索
- 浏览器历史插件
- 全文本搜索（OCR for 图片/PDF）
- 网络驱动器搜索
- 云盘搜索（OneDrive、Google Drive）

---

## 5. 架构设计

### 5.1 总体架构

```
┌─────────────────────────────────────────────────────┐
│                  用户界面层 (UI)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────┐ │
│  │ 搜索浮层/WPF  │  │ Service GUI  │  │ CLI 工具   │ │
│  └──────┬───────┘  └──────┬───────┘  └─────┬──────┘ │
└─────────┼──────────────────┼─────────────────┼───────┘
          │   IPC (Named Pipes / gRPC)          │
┌─────────┼──────────────────┼─────────────────┼───────┐
│  ┌──────┴───────┐  ┌──────┴───────┐  ┌──────┴──────┐ │
│  │ 搜索路由器    │  │ 索引管理器    │  │ 插件管理器  │ │
│  └──────┬───────┘  └──────┬───────┘  └──────┬──────┘ │
│         │                 │                  │       │
│  ┌──────┴─────────────────┴──────────────────┴──────┐ │
│  │              核心引擎层 (Rust)                     │ │
│  │  ┌────────────┐  ┌────────────┐  ┌─────────────┐ │ │
│  │  │ USN 读取器  │  │ 倒排索引    │  │ 拼音匹配器  │ │ │
│  │  ├────────────┤  ├────────────┤  ├─────────────┤ │ │
│  │  │ 文件监控器  │  │ Trie 索引   │  │ 模糊匹配器  │ │ │
│  │  └────────────┘  └────────────┘  └─────────────┘ │ │
│  └───────────────────────────────────────────────────┘ │
│                        │                              │
│  ┌─────────────────────┴─────────────────────────────┐ │
│  │              持久化层 (SQLite)                     │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────┐ │ │
│  │  │ 索引快照      │  │ 搜索历史      │  │ 收藏夹   │ │ │
│  │  └──────────────┘  └──────────────┘  └──────────┘ │ │
│  └───────────────────────────────────────────────────┘ │
│                   核心引擎进程                          │
└───────────────────────────────────────────────────────┘
          │
┌─────────┴───────────────────────────────────────────────┐
│                  Windows 系统层                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ NTFS USN     │  │ ReadDirectory│  │ Win32 API    │  │
│  │ Journal API  │  │ ChangesW     │  │ Shell COM    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 5.2 核心模块设计

#### 5.2.1 USN Journal 读取器 (usn_reader)

- **功能**：读取 NTFS 卷的 USN Journal，获取文件系统变更记录
- **关键 API**：`DeviceIoControl` + `FSCTL_QUERY_USN_JOURNAL`、`FSCTL_READ_USN_JOURNAL`、`FSCTL_ENUM_USN_DATA`
- **增量策略**：
  - 记录上次读取的 USN ID
  - 每次启动时从上次位置继续读取
  - 支持批量读取（每次 64KB 缓冲区）
- **变更类型**：新建、删除、重命名、属性修改、大小修改

#### 5.2.2 索引引擎 (index_engine)

- **数据结构**：
  - **Trie 树**：前缀搜索，内存紧凑
  - **倒排索引**：子串搜索，按路径分段
  - **拼音映射表**：汉字 → 拼音首字母映射
- **存储策略**：
  - 内存中维护完整索引副本
  - SQLite 持久化用于冷启动恢复
  - 增量更新时同步更新内存和磁盘

#### 5.2.3 搜索路由器 (search_router)

- **多源搜索**：同时搜索内存索引和 SQLite
- **结果合并**：去重、排序、评分
- **评分算法**：
  - 精确前缀匹配：+100
  - 子串匹配：+50
  - 当前文件夹内匹配：+30
  - 拼音匹配：+20
  - 模糊匹配：+10
  - 近期访问加分：+5

#### 5.2.4 IPC 层 (ipc)

- **传输**：Windows Named Pipes
- **协议**：JSON-RPC 或 MessagePack 二进制协议
- **接口**：
  - `Search(query, scope, options) → Result[]`
  - `GetCurrentFolder() → path`
  - `GetRecentFiles(n) → path[]`
  - `AddFavorite(path)`
  - `GetFavorites() → path[]`
  - `RebuildIndex()`
  - `GetIndexStatus() → stats`

### 5.3 技术选型

| 模块 | 技术 | 理由 |
|------|------|------|
| 核心引擎 | Rust | 原生性能，无 GC，安全，`windows` crate 直接调用 Win32 API |
| 持久化 | SQLite + rusqlite | 轻量嵌入式，无需独立数据库服务 |
| IPC | Named Pipes + serde_json | 跨进程通信，双向通道，支持流式响应 |
| UI 浮层 | WPF (.NET Framework 4.8) | 成熟的 Windows 桌面 UI 框架，资源丰富 |
| Shell 钩子 | C++/Rust 原生 DLL | Windows 全局钩子需原生 DLL，SetWindowsHookEx |
| 文件监控 | ReadDirectoryChangesW | 目录级实时变更通知 |
| 拼音匹配 | 内置拼音库 | 汉字到拼音首字母映射表，约 6 万条目 |

---

## 6. 数据流

### 6.1 索引构建流程

```
1. 启动 → 检查 SQLite 是否存在索引快照
   ├─ 存在 → 加载到内存 Trie
   ├─ 不存在 → 全盘扫描 → 构建 Trie → 持久化到 SQLite
2. 启动 USN Journal 监控
   ├─ 读取自上次 USN ID 以来的变更
   ├─ 更新内存 Trie
   ├─ 更新 SQLite
3. 持续监听 ReadDirectoryChangesW
   ├─ 文件变更 → 更新内存 Trie
   ├─ 批量变更 → 合并后批量更新
```

### 6.2 搜索流程

```
1. 用户按键 → IPC 发送 SearchRequest
2. 引擎解析查询（识别语法、拼音）
3. 多路搜索（Trie × 拼音 × 倒排）
4. 结果合并、评分、排序
5. 返回 IPC 响应
6. UI 渲染结果列表
```

---

## 7. 项目管理

### 7.1 阶段规划

| 阶段 | 内容 | 预计周期 |
|------|------|---------|
| **P0: 技术验证** | USN Journal API 调研与原型验证，确认 Rust `windows` crate 的 USN 支持 | 1 周 |
| **P1: 引擎核心** | 索引引擎（USN 读取 + Trie + 搜索 + SQLite 持久化）+ CLI 工具 | 3 周 |
| **P2: UI 浮层** | WPF 搜索浮层 + 热键 + 上下文感知 + Quick Switch | 3 周 |
| **P3: Shell 集成** | 资源管理器集成 + 对话框集成 + 插件系统 | 2 周 |
| **P4: 高级功能** | 高级搜索语法 + 类目筛选 + 预览面板 + 收藏夹 | 2 周 |

### 7.2 技术验证项（P0 重点）

1. [ ] Rust `windows` crate 能否调用 `DeviceIoControl` + `FSCTL_QUERY_USN_JOURNAL`
2. [ ] USN Journal 读取性能（100 万条记录耗时）
3. [ ] 内存 Trie 索引占用（100 万文件名）
4. [ ] Named Pipes IPC 延迟（本地回环）
5. [ ] WPF 浮层窗口显示延迟（热键到显示）

---

## 8. 风险与缓解

| 风险 | 影响 | 缓解方案 |
|------|------|---------|
| Rust `windows` crate 对 USN Journal 支持不完整 | 引擎开发受阻 | 备选方案：FFI 调用 Win32 C API 或 C++/CLI 桥接 |
| 无 Visual Studio / Windows SDK | 无法编译原生 C++ 组件 | 纯 Rust 方案，`windows` crate 无需 VS |
| USN Journal 权限不足（非管理员） | 索引不完整 | 降级到 ReadDirectoryChangesW + 定期扫描 |
| 拼音词库准确度 | 拼音搜索效果差 | 内置映射表 + 用户自定义词典 |
| .NET Framework 4.8 逐渐退役 | UI 框架未来兼容性 | 考虑 Rust 原生 UI（egui/iced）或 Tauri 作为备选 |

---

## 9. 附录

### 9.1 术语表

| 术语 | 说明 |
|------|------|
| USN Journal | NTFS 文件系统的更新序列号日志，记录所有文件系统变更 |
| USN ID | 更新序列号，单调递增，标识每次变更 |
| Trie | 前缀树，适用于快速前缀匹配 |
| FST | 有限状态转换器，Trie 的内存优化版本 |
| IPC | 进程间通信 |
| Quick Switch | Listary 的核心功能，在 Save/Open 对话框中快速跳转文件夹 |
| FileAppPlugin | Listary 的插件体系，用于集成第三方文件管理器 |
| Shell Extension | Windows Shell 扩展，用于在资源管理器中嵌入功能 |

### 9.2 参考项目

- **Everything** (voidtools): https://www.voidtools.com/
- **Listary**: https://www.listary.com/
- **Rust `windows` crate**: https://github.com/microsoft/windows-rs
- **DeviceIOControlLib**: https://github.com/bb-Strike/DeviceIOControlLib

### 9.3 已安装环境

| 工具 | 版本 | 路径 |
|------|------|------|
| Rust | 1.96.0 | C:\Users\zhaox\.cargo\bin\rustc.exe |
| .NET Runtime | 8.0.11 | C:\Program Files\dotnet\ |
| .NET Framework | 4.8.1 (Release 533509) | 系统内置 |
| Python | 3.10.15 | D:\Program\envs\llm_base\python.exe |
| Listary | 6.3 | D:\Program Files (x86)\Listary\ |
